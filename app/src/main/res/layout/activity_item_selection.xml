<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">
    <data>
        <variable
            name="specialItemsAdapter"
            type="com.example.android.gameapplication.specialitems.SpecialItemsAdapter" />
    </data>
    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".ItemSelectionActivity">

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/item_selection_rv"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:adapter="@{specialItemsAdapter}"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <fragment
            android:id="@+id/fragment_container_view"
            android:name="com.example.android.gameapplication.ItemFragment"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_constraintEnd_toEndOf="@id/item_selection_rv"
            app:layout_constraintHorizontal_bias="0.0"
            app:layout_constraintStart_toStartOf="@id/item_selection_rv"
            app:layout_constraintTop_toBottomOf="@id/item_selection_rv"
            />

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>